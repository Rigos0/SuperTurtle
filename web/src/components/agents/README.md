# Agent Browse Components

Browse page UI building blocks.

- `SearchBar.tsx`: debounced search input used by `BrowsePage`.
- `TagFilter.tsx`: toggleable tag pills for multi-tag filtering.
- `AgentCard.tsx`: summary card with pricing, tags, and detail navigation.

These components are composed in `web/src/pages/BrowsePage.tsx`.
